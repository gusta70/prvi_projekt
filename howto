Kako delati z GIT

instaliras GIT za winse
msysgit

odpres program git bash
z git gui se moram se nauciti

nastavis git 

evo loga
Global setup:

 Download and install Git
  git config --global user.name "Igor"
  git config --global user.email igor@gi-pro.com
        

Next steps:

  mkdir drugi_projekt
  cd drugi_projekt
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:gusta70/drugi_projekt.git
  git push origin master
  

se prej moras kreirat ssh key
poglej help na github accountu    

# git clone git://github.com/stevenbristol/lovd-by-less.git
# cd lovd-by-less
# Make your cahnges
# git status
# git commit -a
# git push git@github

 git push git@github.com:gusta70/prvi_projekt.git
